name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Running the webapp
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: 34.93.168.214
        username: bitorsic
        password: ${{ secrets.PASSWORD }}
        script: |
          killall node
          cd express-hello-world
          node index.js &
          echo "Access the webapp at http://34.93.168.214"
          exit
